#include<stdio.h>
int input
{
int a
scanf("%d"&a);
}
int compute(int a)
{
a=a+1
return a;
}
void output(int x);
{
printf("%d"(intx));
return;
}
void main()
{
int a,sum;
printf("read a\n");
a=input()
sum=comput(a)
outut=sum
return();
}
